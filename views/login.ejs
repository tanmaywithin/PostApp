<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create User</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 flex items-center justify-center min-h-screen text-white font-sans">
  
  <div class="w-full max-w-md bg-zinc-800 shadow-2xl rounded-2xl p-8">
    <h2 class="text-3xl font-bold mb-6 text-center">Login User</h2>
    
    <form method="post" action="/login" class="space-y-4">
      
      

      <!-- Email -->
      <div>
        <label class="block mb-1 text-sm">Email</label>
        <input type="email" name="email" placeholder="Enter email"
          class="w-full px-4 py-2 bg-zinc-700 border border-zinc-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 transition" required>
      </div>

      <!-- Password -->
      <div>
        <label class="block mb-1 text-sm">Password</label>
        <input type="password" name="password" placeholder="Enter password"
          class="w-full px-4 py-2 bg-zinc-700 border border-zinc-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 transition" required>
      </div>

      

      <!-- Submit Button -->
      <button type="submit"
        class="w-full py-2 bg-blue-600 hover:bg-blue-700 rounded-lg font-semibold transition duration-200">
        Login
      </button>
    </form>

    
     <p class="text-sm text-center text-zinc-400 mt-5">
      Already have an account? 
      <a href="/register" class="text-blue-400 hover:underline">Create New Account</a>
    </p>
  </div>

</body>
</html>
